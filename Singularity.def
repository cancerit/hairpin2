Bootstrap: docker
From: python:3.12-slim

%files
. hairpin/

%post
pip install hairpin/

%test
	LOC=$(which hairpin)
	if [ -z "$LOC"]; then
		echo "hairpin install failed"
	else
		echo "hairpin install successful"
	fi

%runscript
	exec hairpin "$@"
